#Write a script which accepts a sequence of comma-separated numbers from user and generates a list and a tuple with those numbers.
y_list = []

def convert(num):
    values = input("Type values: ")
    
    print(values)
    